apply plugin: 'idea'
apply plugin: 'java'

repositories {
    mavenCentral()
}

dependencies {
    compile 'com.thoughtworks.gauge:gauge-java:+'
}

task runGauge(type: Exec) {
    commandLine "$rootDir/executar"
}

test.finalizedBy(runGauge)
